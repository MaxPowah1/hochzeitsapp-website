RewriteEngine On

# If the requested file or directory doesn't exist, rewrite to paypal.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^.*$ paypal.php [QSA,L]
